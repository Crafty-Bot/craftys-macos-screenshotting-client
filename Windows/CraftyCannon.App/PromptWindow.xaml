<Window x:Class="CraftyCannon.App.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="320" Width="520" MinHeight="220" MinWidth="420"
        WindowStartupLocation="CenterOwner">
    <Grid Background="#F4F5F7" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#20242B" />
        <TextBox x:Name="ValueBox" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Width="92" Height="32" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="OK" Width="92" Height="32" IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
